services:
  postgres:
    image: postgres:16-alpine
    container_name: langlium_db
    restart: unless-stopped
    env_file:
      - ./Backend/.env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: